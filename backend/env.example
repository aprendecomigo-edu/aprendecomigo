# Django environment mode: development, staging, production, testing
DJANGO_ENV=development

# Django secret key - required for all environments except development, which has a default value
# SECRET_KEY=your-secret-key-here

# Allowed hosts - comma separated list of domain names, required for staging/production
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com,staging.yourdomain.com

# Database URL - required for production, optional for staging
# Format: postgresql://user:password@host:port/dbname
# DATABASE_URL=postgresql://user:password@localhost:5432/aprendecomigo

# Email settings
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=your-email@gmail.com
# EMAIL_HOST_PASSWORD=your-email-password
# DEFAULT_FROM_EMAIL=noreply@aprendecomigo.com

# CORS settings - comma separated list of allowed origins
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Google API settings
# GOOGLE_CLIENT_ID=your-google-client-id
# GOOGLE_CLIENT_SECRET=your-google-client-secret

# SMS settings (GatewayAPI)
# SMS_API_URL=https://gatewayapi.com/rest/mtsms
# SMS_API_KEY=your-gatewayapi-key
# SMS_SENDER_ID=AprendeCoM

# Stripe settings (Payment Processing)
# SECURITY WARNING: Use test keys for development/staging, live keys only for production
# Test keys start with sk_test_ and pk_test_
# Live keys start with sk_live_ and pk_live_

# Stripe Secret Key (backend only - NEVER expose in frontend)
# For development/staging: Use test secret key (sk_test_...)
# For production: Use live secret key (sk_live_...)
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key_here

# Stripe Public Key (safe for frontend use)
# For development/staging: Use test public key (pk_test_...)
# For production: Use live public key (pk_live_...)
# STRIPE_PUBLIC_KEY=pk_test_your_stripe_public_key_here

# Stripe Webhook Secret (for webhook signature verification)
# Get this from your Stripe dashboard under Webhooks > [Your Webhook] > Signing secret
# Different for each webhook endpoint you create
# STRIPE_WEBHOOK_SECRET=whsec_your_webhook_signing_secret_here

# Additional Stripe Configuration (Optional)
# Stripe Client ID for OAuth applications (if using Stripe Connect)
# STRIPE_CLIENT_ID=ca_your_stripe_client_id_here

# SECURITY BEST PRACTICES FOR STRIPE:
# 1. NEVER commit real API keys to version control
# 2. Use test keys (sk_test_, pk_test_) for development and staging
# 3. Use live keys (sk_live_, pk_live_) only for production
# 4. Rotate keys regularly and when team members leave
# 5. Monitor Stripe dashboard for unusual activity
# 6. Enable Stripe Radar for fraud protection
# 7. Use webhook signatures to verify authenticity
# 8. Store webhook secrets securely and separately from API keys
