--- a/node_modules/react-dom/cjs/react-dom.development.js
+++ b/node_modules/react-dom/cjs/react-dom.development.js
@@ -2826,7 +2826,15 @@
     if (isCustomProperty) {
       style.setProperty(styleName, styleValue);
     } else {
-      style[styleName] = styleValue;
+      try {
+        style[styleName] = styleValue;
+      } catch (error) {
+        if (error.message && error.message.includes('Indexed property setter is not supported')) {
+          console.warn('Skipped problematic CSS property assignment:', styleName, styleValue);
+        } else {
+          throw error;
+        }
+      }
     }
   }
 }
